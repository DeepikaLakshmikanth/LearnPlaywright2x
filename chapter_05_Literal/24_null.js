let no_audi_pamod_has = null;// This variable is declared and explicitly assigned the null literal, indicating that it has no value or is intentionally empty.
let pramod_has_two_wives;

let age = 25;
let audi = "";
console.log(typeof audi); // Output: "string"
console.log(typeof age); //